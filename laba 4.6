#include <iostream>
#include <cmath>
using namespace std;

int main() {

	setlocale(0, "");

	double a1, b1, c1, a2, b2, c2, p;

	cout << "Программа для решения системы уравнений вида \nA1 * x + B1 * y = C1 \nA2 * x + B2 * y = C2" << endl;

	cout << "Введите коэффициенты A1, B1, C1 : " << endl;

	cin >> a1 >> b1 >> c1;

	cout << "Введите коэффициенты A2, B2, C2 : " << endl;

	cin >> a2 >> b2 >> c2;

	p = a1 * b2 - b1 * a2;

	cout << "Результат : " << endl;

	cout << "x = " << (c1 * b2 - b1 * c2) / p << endl;

	cout << "y = " << (a1 * c2 - a2 * c1) / p << endl;

	cout << endl;

	system("pause");

	return 0;

}
